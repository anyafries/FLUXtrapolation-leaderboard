{
  "name": "fluxtrapolation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: presign R2 uploads + open submission PRs for the FLUXtrapolation leaderboard.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
